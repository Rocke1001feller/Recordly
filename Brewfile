# Required for electron-builder's bundled dmgbuild on macOS.
# electron-builder 26.x ships a prebuilt dmgbuild Python binary that links
# against Homebrew's libintl.8.dylib (part of gettext).
brew "gettext"
